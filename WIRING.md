# 智能停车场栏杆 — 接线图

## 硬件清单

| 序号 | 组件 | 型号 | 数量 |
|------|------|------|------|
| 1 | 树莓派 | Raspberry Pi 4 Model B | 1 |
| 2 | ADC 模块 | PCF8591 | 1 |
| 3 | 光敏传感器 | 光敏电阻模块 (接 PCF8591) | 1 |
| 4 | 超声波模块 | HC-SR04 | 1 |
| 5 | 舵机 | SG90 / MG996R | 1 |
| 6 | 蜂鸣器 | 有源蜂鸣器 (高电平触发) | 1 |
| 7 | 电阻 | 2.2kΩ + 3.3kΩ (Echo 分压) | 各 1 |
| 8 | 杜邦线 | 公母/母母 | 若干 |
| 9 | 面包板 | — | 1 |

---

## 树莓派 GPIO 引脚对照 (Pi 4 Model B)

```
                    3.3V (1)  (2)  5V
                I2C SDA (3)  (4)  5V
                I2C SCL (5)  (6)  GND
                        (7)  (8)  GPIO14
                       GND (9)  (10) GPIO15
                 GPIO17 (11) (12) GPIO18  ← PCF8591 SDA
                 GPIO27 (13) (14) GND
                 GPIO22 (15) (16) GPIO23
                   3.3V (17) (18) GPIO24
                 GPIO10 (19) (20) GND
                  GPIO9 (21) (22) GPIO25
                  GPIO0 (23) (24) GPIO8
                  GPIO7 (25) (26) GPIO1
                  GPIO2 (27) (28) GPIO3
                  GPIO4 (29) (30) GND
     ← HC-SR04 Echo GPIO6 (31) (32) GPIO12  HC-SR04 Trig →
                 GPIO13 (33) (34) GND
     ← 舵机信号 PWM1 GPIO19 (35) (36) GPIO16
                 GPIO26 (37) (38) GPIO20
                       GND (39) (40) GPIO21  蜂鸣器 →
```

---

## 详细接线

### 1. PCF8591 + 光敏传感器 (I2C)

| PCF8591 引脚 | 连接目标 | 说明 |
|-------------|----------|------|
| VCC | 树莓派 3.3V (物理引脚 1) | 供电 |
| GND | 树莓派 GND (物理引脚 6) | 共地 |
| SDA | 树莓派 GPIO18 (物理引脚 12) | I2C 数据线 |
| SCL | 树莓派 GPIO17 (物理引脚 11) | I2C 时钟线 |

**光敏传感器模块 → PCF8591:**

| 光敏模块 | PCF8591 | 说明 |
|---------|---------|------|
| VCC | VCC 或 3.3V | 供电 |
| GND | GND | 共地 |
| AO | AIN0 | 模拟输出 |

> **注意:** 需要在 `/boot/config.txt` 中添加 `dtoverlay=i2c-gpio,i2c_gpio_sda=18,i2c_gpio_scl=17,bus=3` 启用软件 I2C。

---

### 2. HC-SR04 超声波

| HC-SR04 引脚 | 连接目标 | 说明 |
|-------------|----------|------|
| VCC | 树莓派 **5V** (物理引脚 2) | ⚠️ 需 5V 供电 |
| GND | 树莓派 GND (物理引脚 9) | 共地 |
| Trig | 树莓派 GPIO12 (物理引脚 32) | 触发脉冲 |
| Echo | **分压后** 接 GPIO6 (物理引脚 31) | ⚠️ 回波 5V→3.3V |

**Echo 分压电路 (必须!):**

```
HC-SR04 Echo ──┬──[ 2.2kΩ 电阻 ]──┬── GPIO6 (树莓派)
               │                  │
               │                  │
               │            [ 3.3kΩ 电阻 ]
               │                  │
               │                  │
               └──────────────────┴── GND
```

> **不接分压会烧树莓派 GPIO!** Echo 输出 5V，树莓派 GPIO 只能承受 3.3V。

---

### 3. 舵机 (栏杆)

| 舵机线色 | 连接目标 | 说明 |
|---------|----------|------|
| 棕/黑 (GND) | 树莓派 GND (物理引脚 34) | 共地 |
| 红 (VCC) | 树莓派 5V (物理引脚 4) | 供电 (小舵机可，大舵机需外接电源) |
| 橙/黄 (信号) | 树莓派 GPIO19 (物理引脚 35) | PWM 控制 |

> **注意:** 需要在 `/boot/config.txt` 中添加 `dtoverlay=pwm,pin=19,func=2` 启用 PWM。

---

### 4. 蜂鸣器

| 蜂鸣器引脚 | 连接目标 | 说明 |
|-----------|----------|------|
| VCC | 树莓派 3.3V (物理引脚 17) | 供电 |
| GND | 树莓派 GND (物理引脚 39) | 共地 |
| I/O (信号) | 树莓派 GPIO21 (物理引脚 40) | 高电平触发 |

---

## 总接线一览

```
                      ┌─────────────────────────────┐
                      │     Raspberry Pi 4           │
                      │                             │
  PCF8591 ─────────── │ GPIO17 (11) ← SCL          │
  PCF8591 ─────────── │ GPIO18 (12) ← SDA          │
  HC-SR04 ─────────── │ GPIO12 (32) ← Trig         │
  HC-SR04 ──[分压]── │ GPIO6  (31) ← Echo         │
  舵机 ───────────── │ GPIO19 (35) ← PWM          │
  蜂鸣器 ─────────── │ GPIO21 (40) ← 信号          │
                      │                             │
  3.3V ────────────── │ Pin 1/17  (PCF8591, 蜂鸣器)│
  5V   ────────────── │ Pin 2/4   (HC-SR04, 舵机)  │
  GND  ────────────── │ Pin 6/9/14/20/30/34/39     │
                      └─────────────────────────────┘
```

## 系统启动命令

```bash
# 1. 启用 I2C (GPIO 17/18) — WiringPiI2C 需要
sudo dtoverlay i2c-gpio i2c_gpio_sda=18 i2c_gpio_scl=17 bus=3

# 2. 运行程序 (softPwm 无需额外配置)
cd ~/Cproject
sudo ./parking_radar
```

## 软件架构 (v4.0 WiringPi)

```
parking_radar.c
├── wiringPi              → GPIO: 超声波 Trig/Echo, 蜂鸣器
├── wiringPiI2C           → I2C: PCF8591 光敏传感器
└── softPwm               → PWM: 舵机 (GPIO19, 20ms周期)
```

**状态机 (夜晚模式):**

```
WAIT ──车来(<80cm)──▶ CAR_HERE ──车离开──▶ CLOSING(3s倒计时)
  ▲                                                  │
  └──────────── COOLDOWN(3s冷却) ◀──────────────────┘
```
